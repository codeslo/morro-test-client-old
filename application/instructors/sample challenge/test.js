// do not change code in this file except for testName
const testName = 'My Test';
const root = require('app-root-path');
const configRunner = require(root + '/config/configurator');
configRunner(testName);
